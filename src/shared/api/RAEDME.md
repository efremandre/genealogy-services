``` shared/api ```

* базовый API-клиент  
* функции для подстановки Bearer token  
* общая обработка ошибок  
* типы ответов API, если хочешь  

``` shared/api/client ```  

##### Отвечает только за:  
* отправить HTTP-запрос  
* если надо, взять токен и добавить Authorization  

Это транспортный слой.  